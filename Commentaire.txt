Bug connu : Crash lors de la libération de zelda lorsque l'on quitte l'application

utilisation de kr_collision entité :
 mettre les lignes suivante après la détéction d'obstacles:
 '
Uint32 i = 0;
		Entity **aEntity = pLevelSt->aEntityLevel;
		for (i = 1; i < pLevelSt->iNbEntities + 1; i++){
			if (Kr_CollisionEntity(pLevelSt->pLevel, (*(aEntity + i))->pSprEntity->pRectPosition, movex, movey) == TRUE){
				//infightingDamage(*(aEntity + i), pPlayer);
				movex = movey = 0;
			}
		}
